ADMIN_SERVER_PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=mora9s_2026
JWT_SECRET=super_secret_key

# Base URL of the user-mode API + Socket.IO server (same origin browsers use as VITE_API_URL).
# Required in production so admin can relay real-time updates to the public site.
USER_SERVER_URL=http://localhost:4000
